events {
   worker_connections 1024;
    # Event settings
}

http{
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

# server {
#     listen 80;
#     server_name test.hillpostmfb.com maxiheinzsaxphone705.hillpostmfb.com;
#     location / {
#        return 301 https://$host$request_uri;
#     }
# }

server {
    listen 8080; #ssl;
    server_name _; #test.hillpostmfb.com maxiheinzsaxphone705.hillpostmfb.com;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        proxy_pass http://192.168..154:3000;  # The name of your Django service (web) and port
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Authorization $http_authorization;
        client_max_body_size 10M;
        proxy_connect_timeout 600;
        proxy_send_timeout 600;
        proxy_read_timeout 600;
        send_timeout 600;
    }

    location ~* \.(css|js|jpg|png|gif|ico|svg)$ {
        root /usr/share/nginx/html;
        expires 1h;
        add_header Cache-Control "public";
    }
    # location swagger/ {
    #     # allow 192.168.0.122; 
    #     proxy_pass http://127.0.0.1:3000/;
    # }
    # ssl_certificate /etc/nginx/certs/hillpost.crt;  # Path to your SSL certificate
    # ssl_certificate_key /etc/nginx/certs/hillpost.key;
    # ssl_protocols TLSv1.2 TLSv1.3;
    # ssl_ciphers 'TLS_AES_128_GCM_SHA256:TLS_AES_256_GCM_SHA384:TLS_CHACHA20_POLY1305_SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384';
    # ssl_prefer_server_ciphers off;

}


}